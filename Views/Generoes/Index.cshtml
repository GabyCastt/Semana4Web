@model IEnumerable<BibliotecaBDD.Models.Genero>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Registro Géneros</h1>

<p>
    <a asp-action="Create">Nuevo Género</a>
</p>
<table id="myTable" class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EdadRecomendada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Popularidad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EdadRecomendada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Popularidad)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-outline-warning" asp-route-id="@item.IdGenero">Edit</a> |
                    <a asp-action="Details" class="btn btn-outline-info" asp-route-id="@item.IdGenero">Details</a> |
                    <a asp-action="Delete" class="btn btn-outline-danger" asp-route-id="@item.IdGenero">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
